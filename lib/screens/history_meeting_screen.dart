import 'package:flutter/material.dart';

class HostoryMeetingScreen extends StatelessWidget {
  const HostoryMeetingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
